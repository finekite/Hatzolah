@model Hatzloah.Models.HatzolahNocModel

    <p><b>NOC: </b><span id="final-noc-code">@Model.HatzolahNoc.Noc</span></p>
    <p><b>TONE: </b>@Model.HatzolahNoc.Tone</p>
    <p><b>INSTRUCTIONS: </b> @Model.HatzolahNoc.Instructions</p>
    @if (Model.HatzolahNoc.CodeOneFlag)
    {
        @Html.Hidden("true", "true", new { id = "id-code-1"})
    }
    else
    {
        @Html.Hidden("false", "false", new { id = "id-code-1" })
    }

    @if (Model.HatzolahNoc.Question != null && Model.HatzolahNoc.Question != string.Empty)
    {
        <div id="question" class="row" hidden>
            <div class="col-lg-9">
                <p>@Model.HatzolahNoc.Question.ToUpper()</p>
            </div>
            <div class="col-lg-3">
                <input id="input-for-additional-questions" class="form-control" type="text" />
            </div>
        </div>
    }

